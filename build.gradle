apply plugin: 'java'
apply plugin: 'idea'

repositories{
  mavenCentral()
}

dependencies{
      compile('org.springframework:spring-context:3.2.0.RELEASE')
}

task createDirs << {
      sourceSets*.java.srcDirs*.each{it.mkdirs()}
}
